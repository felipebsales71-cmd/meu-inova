{
  "name": "meu-inova-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Meu Inova - portal academico e administrativo em Cloudflare Workers + D1",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
